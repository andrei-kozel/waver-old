{
  "name": "waver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend:install": "cd ./frontend && npm install",
    "frontend:dev": "cd ./frontend && npm run start",
    "frontend:test": "cd ./frontend && npm run test",
    "frontend:build:windows": "cd ./frontend && npm run build && cd .. && move frontend/build build",
    "frontend:build:unix": "cd ./frontend && npm run build && cd .. && mv frontend/build build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.2",
    "hardhat": "^2.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
